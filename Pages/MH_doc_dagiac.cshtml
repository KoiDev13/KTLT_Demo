@page
@model KTLT_Demo.Pages.MH_doc_dagiacModel
@using KTLT_Demo.Business
@using KTLT_Demo.DAL;
@using KTLT_Demo.ENTITY;  
@{
    Layout = null;
    string Chuoi = "";

    //DAGIAC dg = LT_DAGIAC.DocDaGiac("d:\\dagiac.txt");
    DAGIAC dg = LT_DAGIAC.DocDaGiac("d:\\dagiac.json");

    Chuoi = string.Empty;
    foreach (DIEM d in dg.Dinh)
    {
        Chuoi = Chuoi + $"({d.X},{d.Y})";
    }
}
<!DOCTYPE html>
<html>
<body>
    @foreach (DIEM d in dg.Dinh)
    {
    <div>(@d.X, @d.Y)</div> 
    }
</body>
</html>
